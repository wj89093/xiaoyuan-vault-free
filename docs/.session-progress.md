
## 2026-05-24 (晚) — Agent 记忆系统 Phase 2 + 窗口 IPC

### 记忆系统 Phase 2 完成

**P1 — 系统 prompt 记忆说明**
- `wikiSchema.ts`: 新增「记忆系统」章节（何时用 queryMemory、记忆分层）
- `SelfAgentAdapter.streamPrompt()`: 把 `this._systemPrompt`（记忆上下文）注入 vault system prompt

**P2 — 问候语注入记忆概要**
- `buildDailyGreeting()`: async，接收 `memoryContext` 参数
- 问候语新增「📋 最近：」标签，显示最近讨论话题
- `sessionManager.ts`: 加载记忆 → 生成问候语（顺序调整）
- 测试修复：6个 `buildDailyGreeting` 测试改为 async + await

**P3 — BriefingPanel 数据通道**
- 问题：`window.api.getConversations` 在 legacy flat aliases 中缺失
- 修复：`preload/index.ts` 添加 `getConversations` alias → `maintenance.getConversations`
- BriefingPanel 现在能正常读取 `_briefing/conversations/YYYY-MM-DD/` 数据

**P4 — briefing.ts 旧代码评估**
- `saveConversationSummary()`（briefing.ts）vs `saveSessionSummary()`（agentMemory.ts）
- 分析结论：briefing.ts 中大部分是死代码，但保留有备份价值（万一需要手动调用）
- 实际使用：`sessionManager` → `saveSessionSummary`（agentMemory.ts）

### 窗口控制 IPC 修复

**新增** `aiChatWindow.ts`:
- `aiChat:setWindowSize` — 设置窗口宽高
- `aiChat:getWindowSize` — 获取窗口宽高（同步）

**已有**：
- `aiChat:move` — 相对移动
- `aiChat:moveToCorner` — 移到屏幕角落
- `aiChat:minimize` / `aiChat:close`

### Build & Test
- Build ✅ 5.50s
- Test ✅ 199 passed / 1 skipped

### 残留项（低优先级）
- [ ] `chat:abort` 后 `_currentRecord` 未清理 — 分析结论：设计如此（abort 后继续复用 session）
- [ ] briefing.ts 死代码 — 保留有备份价值
